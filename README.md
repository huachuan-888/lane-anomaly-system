# 车道线异常智能归因系统 (Lane Anomaly Attribution System)

基于 **D760 王者6x4 自主一代 L2+ 智驾项目** 的车道线异常自动化分析平台。
从测试问题表出发，自动完成「数据定位 → 异常检测 → 视频抽帧 → 清晰度评估 → YOLOP双维度校验 → 规则引擎归因 → 报告输出」全流程。

---

## 🧩 功能全景（已实现）

### 1. Web 系统（Flask，浏览器操作，端口 5000）

| 功能 | 说明 |
|------|------|
| **仪表盘** | 统计卡：问题总数 / 车道线问题 / 蛇形问题 |
| **问题列表** | 33 个问题，含场景标签（隧道/弯道/分合流/昼夜/天气）|
| **单问题深度分析** | 基本信息 + 错误点 + b1±1s 三张抽帧 + 清晰度 + 双维度校验 + 丢失事件链 |
| **前后翻页** | 左右两侧悬浮按钮，滚动中随时切换上一个/下一个问题 |
| **📤 文件上传** | 网页拖拽/点击上传 问题表(xlsx) / CSV / 视频(ts)，视频自动按日期归档 |
| **⚡ 一键复核** | 调用命令行流水线跑全量分析，生成最新报告 |
| **性能优化** | 分析结果缓存（翻页秒开）+ 视频帧缓存 + 图片懒加载 + 错误点折叠 + 加载提示 |

### 2. 命令行流水线（车道线自动复核.py）

| 模块 | 功能 |
|------|------|
| ① 关键字筛选 | 自动筛选 车道线/压线/蛇形 相关问题行（+同义词）|
| ② CSV 数据定位 | 按时间戳匹配 106 个 CSV（10分钟窗口）|
| ③ 错误点扫描 | 5 类异常：缺失 / 抖动 / 过宽 / 过窄 / 可视范围骤降 |
| ④ 视频定位 | 按日期子文件夹 + 文件名时间戳匹配 TS 视频 |
| ⑤ 视频抽帧 | 顺序读帧（TS 跳帧不可靠）+ 3 帧补偿，b1±1s 三张 |
| ⑥ 清晰度评估 | S = w1·L + w2·E + w3·C + w4·B + w5·F（权重可配）|
| ⑦ 场景识别 | 隧道 / 弯道 / 分合流 / 昼夜 自动标注 |
| ⑧ 双维度校验 | **YOLOP 语义分割实测宽度** vs CSV 上报 → 自动归因 |
| ⑨ 规则五事件链 | 丢失聚合 + 前兆关联 + 恢复时长 |
| ⑩ 报告生成 | HTML（含异常趋势图 + 折叠卡片）+ Markdown 双格式 |

### 3. 规则引擎（对齐判断标准.txt + PPT）

| 规则 | 判定条件 |
|------|---------|
| 规则1 车道线缺失 | id=1（左）或 id=-1（右）不在检测列表 |
| 规则2 异常抖动 | \|C0(t) − C0(t−1)\| ≥ 0.2 且连续 3 帧 |
| 规则3 宽度异常 | 总宽 ≤3.4m 过窄；≥4.0m 预警；≥4.1m 持续5帧降级 |
| 规则4 可视范围 | 从 ≥60m 骤降 <30m；或持续 <30m 超 10 帧 |
| **规则5 丢失事件链** | 同侧丢失帧间隔≤2s 聚合为一次事件；丢失前≤5s 有前兆 → "有前兆的丢失"；否则"突发丢失"；记录恢复时长 |

### 4. 车道线检测（YOLOP 深度学习）

| 功能 | 说明 |
|------|------|
| **语义分割** | YOLOP 模型：车道线 + 可行驶区域 + 目标检测（34MB ONNX）|
| **CLAHE 增强** | 适配画面偏暗场景（亮度峰值<150）|
| **单侧互补** | YOLOP 检到单侧时，另一侧用 Hough 补 |
| **置信度分级** | 车道线像素 <3000 → 判定"检测不可靠"，归入人工复核 |
| **模型回退** | 模型缺失时自动回退 Hough（仅提示）|

### 5. 双维度校验（核心创新，YOLOP 版）

```
CSV 报异常 + YOLOP 实测宽度 <4.0m   → ⚠️ 疑似感知误报
CSV 报异常 + YOLOP 实测宽度 ≥4.0m   → ✅ 真实异常
CSV 报丢失 + 画面检测到车道线         → ⚠️ 感知漏检
CSV 报丢失 + 画面未检测到            → ✅ 真实丢失
检测不完全 / 像素不可靠               → 🔍 需人工复核
```

---

## 🚀 快速开始

### 依赖

```bash
pip install openpyxl flask opencv-python numpy onnxruntime
```

### 下载模型（34MB）

```
https://github.com/huachuan-888/lane-anomaly-system/releases/download/v1.0-models/yolop-640-640.onnx
放到 models\yolop-640-640.onnx（无模型自动回退 Hough）
```

### 启动 Web 系统

```powershell
cd 车道线异常智能归因系统
python app.py
# 浏览器打开 http://127.0.0.1:5000/
```

或双击 `启动系统.bat`（自动打开浏览器，带端口检测/重启菜单）。

### 命令行全量复核

```powershell
python 车道线自动复核.py
# 输出到 自动复核输出/reports/错误点复核报告.html
```

---

## 🖥️ 网页版使用教程

### 启动

```powershell
cd 车道线异常智能归因系统
python app.py
```

浏览器打开 **http://127.0.0.1:5000/**（或双击 `启动系统.bat`，自动打开浏览器）。

### 页面功能

```
┌──────────────────────────────────────────────┐
│ 🚗 车道线异常智能归因系统                      │
│ ┌──────┬──────┬──────┬──────┐                │
│ │问题33 │车道线20│纯车道│蛇形2 │ ← 统计卡     │
│ └──────┴──────┴──────┴──────┘                │
│ [📤 上传文件] [⚡ 一键复核] [📄 查看报告]       │
│ ──────────────────────────────────────────── │
│ 📋 问题列表 (33)                              │
│  # 日期 时间 归类 场景 描述 [分析详情→]        │
└──────────────────────────────────────────────┘
```

### 操作步骤

**① 查看问题列表**
- 首页显示全部问题（场景标签：隧道/弯道/昼夜/天气）
- 点任意问题行的 **「分析详情→」** 进入深度分析页

**② 单问题深度分析**
- **基本信息**：问题描述、日期时间、关联 CSV/视频
- **错误点列表**：每个错误点 b1 带 3 张抽帧（b1-1s / b1 / b1+1s）+ 清晰度 S + 双维度校验结论
- **丢失事件链**（规则五）：🔵有前兆 / 🔴突发 丢失事件
- **翻页导航**：屏幕左右两侧悬浮 ⬅➡ 按钮，滚动中随时切换上一个/下一个问题

**③ 上传新数据**
- 点 **「📤 上传文件」**，三个拖拽框：
  - 📋 问题表（.xlsx）→ 替换旧问题表
  - 📊 CSV → 存入数据目录
  - 🎥 视频（.ts）→ 自动按日期归档到 `视频/6.16|6.17|6.18/`
- 上传完成后刷新页面即可看到新数据

**④ 一键复核**
- 点 **「⚡ 一键复核」** → 后台跑全量分析（约几分钟）→ 生成最新报告

**⑤ 查看完整报告**
- 点 **「📄 查看报告」** → 打开 HTML 分析报告（趋势图 + 折叠卡片）

---

## 💻 命令行流水线使用教程

### 全量分析（默认）

```powershell
python 车道线自动复核.py
```

自动完成：筛选问题 → 定位 CSV → 扫描错误点 → 定位视频 → 抽帧 → 清晰度 → 双维度校验 → 生成报告

### 常用参数

```powershell
# 指定数据根目录（默认 CONFIG 里的 base_dir）
python 车道线自动复核.py --base "D:\数据目录"

# 扩大错误点扫描窗口（默认 ±30s）
python 车道线自动复核.py --window 60

# 限制每问题最多错误点数（默认 25）
python 车道线自动复核.py --max-points 10

# 自定义筛选关键词（默认 车道线/压线/蛇形）
python 车道线自动复核.py --keywords "车道线,压线"
```

### 输出结果

```
自动复核输出/
├── reports/
│   ├── 错误点复核报告.html    ← 可视化报告（浏览器打开）
│   └── 错误点复核报告.md      ← Markdown 版
├── frames/                   ← 抽帧截图（b1±1s 三张/点）
└── logs/                     ← 运行日志
```

### 命令行参数速查

| 参数 | 默认 | 说明 |
|------|------|------|
| `--base` | CONFIG.base_dir | 数据根目录 |
| `--window` | 30 | CSV 扫描窗口 ±秒 |
| `--max-points` | 25 | 每问题最大错误点数 |
| `--keywords` | 内置 | 筛选关键词（逗号分隔）|

---

## 📁 文件结构

```
├── app.py               # Flask Web 系统主程序
├── templates/
│   ├── index.html       # 首页（仪表盘+问题列表+上传）
│   └── problem.html     # 单问题深度分析页（悬浮导航+事件链）
├── 启动系统.bat          # 一键启动脚本
├── 车道线自动复核.py      # 命令行批量复核流水线
├── 判断标准.txt          # 异常判定规则依据（规则一~五）
└── 车道线分析工具/
│   ├── index.html       # 离线分析工具
│   └── analyze_pipeline.py
└── skills/
    └── html-visual-report/   # Hermes AI Agent 技能（见下节）
        ├── SKILL.md          # 技能说明书
        ├── scripts/
        │   ├── generate_report.py   # 技能入口（--data/--out）
        │   ├── reference_engine.py  # 完整分析引擎
        │   └── setup_env.py         # 环境自动配置
        ├── references/
        │   ├── csv-video-time-alignment.md
        │   └── 判断标准.txt
        └── assets/report_style.css
```

---

## 🤖 Hermes AI Agent 技能（html-visual-report）

> **新增技能**：把整套分析流程封装成 Hermes 技能，一句话即可生成与 Web/命令行**同款效果**的 HTML 报告。

### 功能

| 能力 | 说明 |
|------|------|
| **问题表驱动** | 读 xlsx（33问题→20车道线相关→211错误点）|
| **完整分析** | CSV规则检测 + 视频抽帧（322张）+ 双维度校验 + 规则五事件链 |
| **场景识别** | 天气（晴/阴/小雨）+ 道路环境（隧道/弯道/分合流/鱼骨线）|
| **画面侧丢失** | 画面只检测到单侧线 → 追加 `画面X侧丢失` 独立标签 |
| **时间对齐** | 截图时间与问题时间逐秒对应（b0s=问题时刻）|
| **自包含输出** | 单个 HTML 文件（base64 内嵌，离线可开）|

### 使用

```powershell
# 方式A: 指定数据根目录（含 xlsx/CSV/视频）
python skills/html-visual-report/scripts/generate_report.py --data "数据根目录" --out "报告.html"

# 方式B: 上传数据（保存到本地后同方式A）
# 方式C: 直接对 Hermes 说 "生成报告" / "分析这批车道线数据"
```

### 安装到 Hermes

```powershell
# 把 skills/html-visual-report 复制到 Hermes 技能目录:
#   <hermes-home>/skills/html-visual-report/
# 或通过 Hermes UI 的技能管理加载
```

---

## 🔧 数据目录约定

| 数据 | 位置 | 格式 |
|------|------|------|
| 问题表 | 数据根目录 | `V1.1.6版本测试问题.xlsx` |
| CSV | `同类型CSV_lane_mark_camera_list_1/` | `日期_时-分-秒...csv` |
| 视频 | `视频/6.16|6.17|6.18/` | `ND02512_HHMMSS_vedio_chn4.ts` |
| 视频↔CSV对应 | `视频CSV对应/YYYY-MM-DD_HH-MM-SS/` | 每组 1 TS + 1 CSV |

上传文件会自动归档到对应目录。

---

## ⚠️ 技术要点（踩过的坑）

1. **TS 视频跳帧不可靠**：`cap.set(POS_FRAMES)` 对 TS 无效，必须顺序 `read()` 逐帧数
2. **开头帧补偿**：视频开头约 3 帧缓冲，帧号需 +3：`帧号=(b1秒−起始秒)×25+3`
3. **水印时间不可信**：开头含旧素材帧（06/07），对齐必须以 CSV 时间为准
4. **双维度校验**：画面清晰 ≠ 画面正常，必须检测实际车道线宽度（YOLOP），Hough 在合流口/暗光下会误判
5. **同一秒标签聚合**：每类型只保留最严重值（宽度 max / 可视 min），禁用字符串比较（Unicode 排序坑）
6. **函数内 import cv2**：否则 OpenCV 静默回退 Hough（YOLOP 检测失效）

---

## 📌 未实现 / 待优化

### 未实现

| 功能 | 原因 |
|------|------|
| Rosbag → CSV 自动转换 | 当前数据已是 CSV，无原始 bag |
| YOLOP 全量帧检测 | 每帧 1-2s（CPU），目前只用于校验帧 |
| 弯道车道线拟合 | Hough 直线拟合局限，需多项式/分段拟合 |
| 压线问题检测验证 | 当前数据无压线样本 |

### 可优化

| 方向 | 建议 |
|------|------|
| 性能 | 分析改后台异步任务 + 视频预加载（翻页更快）|
| 可视化 | 抽帧加车道线标注（多轮迭代未达满意，已暂停）|
| 导出 | 错误点表格导出 Excel |
| 配置 | CONFIG 外置为 yaml/JSON，免改代码 |
| 测试 | scan_errors / dual_check 单元测试 |
| 多问题对比 | 同视频多问题并排分析 |

---

## 📊 实测结果（V1.1.6 数据，YOLOP 版）

- 33 个问题，筛选出 20 个车道线/蛇形相关问题
- 检出 211 个错误点，322 张视频截图
- 双维度校验（YOLOP）：**15 真实异常 / 62 疑似误报 / 32 需人工复核**（Hough 版为 4/81/21——YOLOP 纠正了 9 个被 Hough 误判的真实异常）
- 规则五事件链：检出 1 条（#16 右侧丢失 2s，有前兆）
- 主要异常类型：车道线过宽 153 次 / 过窄 41 次 / 抖动 16 次

---

## 📄 License

MIT


---

## 🖥️ 桌面版 exe（免安装）

把 Web 系统打包为独立 exe（PyInstaller），**无需安装 Python**，双击即用：

`
桌面版exe/车道线归因系统/
├── 车道线归因系统.exe   ← 双击启动
├── _internal/           ← 运行库（与exe一起拷贝）
└── 数据/                ← 数据放这里（可选，自动探测）
    ├── V1.1.6版本测试问题.xlsx
    ├── 同类型CSV_lane_mark_camera_list_1/
    └── 视频/
`

- **自动探测数据**：exe 启动时自动找「exe旁/数据/」→「exe所在目录」→ 旧路径
- **自动打开浏览器**：启动 2 秒后自动打开 http://127.0.0.1:5000/
- **任意目录结构**：CSV/视频自动递归发现，不依赖固定路径
- **展开全部错误点**：默认折叠前3个，点击「展开全部」显示所有错误点（含截图）

## 📦 命令行版本（三方案）

| 方案 | 命令 | 说明 |
|------|------|------|
| 全量复核 | python 车道线自动复核.py | 问题表驱动，自动检测数据目录 |
| 单CSV分析 | python 车道线分析工具/analyze_pipeline.py | 自动检测当前目录CSV+匹配视频 |
| Hermes技能 | skills/html-visual-report/ | AI自动生成报告 |


---

## 📚 交付文档

| 文档 | 说明 |
|------|------|
| [docs/系统开发说明.md](docs/系统开发说明.md) | 系统开发历程、架构、五大规则、实测结果 |
| [docs/系统部署文档.md](docs/系统部署文档.md) | 三种部署方法（exe/Python/Hermes技能）分步教程 |
